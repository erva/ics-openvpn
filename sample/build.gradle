apply plugin: 'com.android.application'

android {
  def version = rootProject.extensions.ext.versions
  compileSdkVersion version.compileSdkVersion

  defaultConfig {
    applicationId "io.erva.sample"
    minSdkVersion version.minSdkVersion
    targetSdkVersion version.targetSdkVersion

    versionName = version.clientVersionName
    versionCode = version.clientVersionCode
  }

  buildTypes {
    release {
      minifyEnabled false
      proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
    }
  }

  compileOptions {
    sourceCompatibility = JavaVersion.VERSION_1_8
    targetCompatibility = JavaVersion.VERSION_1_8
  }
}


dependencies {
    implementation fileTree(include: ['*.jar'], dir: 'libs')
//    implementation project ('ovpn_client-0.7.10.163.1')
    implementation fileTree(dir: 'libs', include: ['*.aar'])
    implementation project(':client')
//    implementation project(path: ':ovpn_client-0.7.10.163.1')


}
